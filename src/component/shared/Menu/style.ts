import {Menu} from 'react-native-paper';
import styled from 'styled-components/native';

export const MenuContainer = styled(Menu).attrs({})`
  margin: 0px;
  height: auto;
`;

export const MenuItem = styled(Menu.Item).attrs({})``;
